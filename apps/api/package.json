{
  "name": "@quietpact/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@quietpact/chain-records": "workspace:*",
    "@quietpact/domain": "workspace:*",
    "@quietpact/envelope": "workspace:*",
    "fastify": "^5.0.0",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@quietpact/invoice": "workspace:*",
    "tsx": "^4.0.0"
  }
}
