{
  "name": "@quietpact/invoice",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false",
    "test": "vitest run"
  },
  "dependencies": {
    "@quietpact/domain": "workspace:*",
    "@quietpact/envelope": "workspace:*",
    "viem": "^2.0.0"
  }
}
